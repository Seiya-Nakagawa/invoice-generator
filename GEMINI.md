# GEMINI.md

このファイルは、本リポジトリで作業する AI アシスタントへのガイダンスです。

## 1. 基本方針

- 文字コード: **UTF-8**
- 改行コード: **LF**
- 言語: 日本語を基本とする。変数名・関数名などのコード識別子は英語を使用する。
- **AI の応答は日本語で行う。**

## 2. 開発ワークフロー (GAS)

Google Apps Script (GAS) の開発においては、以下の手順を厳守すること。

1. **GAS への反映**: 修正が完了したら、まず `clasp push -f` を実行して GAS 環境へ反映する。このステップは AI が自動的に実行して良いものとする。
   - もし権限エラー (`The caller does not have permission`) が発生した場合は、`clasp login --no-localhost` を実行し、ブラウザサブエージェントを使用して認証を完了させること。
2. **動作確認依頼**: **ユーザーに対してスプレッドシート上での動作確認を依頼し、その結果を待つ。** AI は UI 操作ができないため、このステップを省略してはならない。
3. **Git への反映**: **ユーザーから動作確認完了（OK）の連絡を受けた後にのみ**、`git add`、`git commit`、`git push` を行う。動作確認前に GitHub へプッシュすることは厳禁とする。本プロジェクトにおいては、作業効率化のためブランチ作成を省略し `main` ブランチへ直接プッシュすることを許容する。

## 3. 機密情報の取り扱い

- API キー、パスワード、接続情報などの機密情報は、コード内に直接記述することを**厳禁**とする。
- 機密情報は環境変数やスクリプトプロパティを使用するか、リポジトリに含めない設定ファイルで管理する。
- `.env` や `credentials.json` などのファイルは絶対にコミットしない。

## 4. 各種規約

- **Git コミットメッセージ**: Conventional Commits に準拠する。
- **ドキュメント同期**: `mail_invoice/` 配下のコードを変更した場合は、必要に応じて `docs/` 配下の要件定義書・設計書も更新する。
